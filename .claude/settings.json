{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | grep -q '\\.rs$' && make fmt >/dev/null 2>&1 || true",
            "statusMessage": "Running cargo fmt"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "output=$(make clippy 2>&1) || echo \"$output\" | jq -Rs '{decision:\"block\", reason:(\"Clippy failed - fix before stopping:\\n\" + .)}'",
            "statusMessage": "Running cargo clippy",
            "timeout": 300
          }
        ]
      }
    ]
  }
}
